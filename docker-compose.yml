services:
  wiki:
    build: .
    container_name: antonlabswiki
    restart: unless-stopped
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - PORT=3001
      - UPLOADS_DIR=/uploads
    ports:
      - "3001:3001"
    volumes:
      - ./uploads:/uploads